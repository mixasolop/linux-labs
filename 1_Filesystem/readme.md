# Filesystem Lab (Linux C)

A small example that deals with basic filesystem operations
on Linux using C.

## What this lab shows
The program demonstrates:
- Basic file operations: opening, reading, writing
- Using `open`, `read`, `write`, `close`
- Checking return values and error handling
- Working with file descriptors

## How to build
make

### How to run
./ops-drive
